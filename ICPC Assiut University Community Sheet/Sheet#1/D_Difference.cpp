#include <bits/stdc++.h>
using namespace std;

using ll = long long;

int main(){
    ll a, b, c, d;
    cin >> a >> b >> c >> d;
    ll x = (a * b) - ( c * d);
    cout << "Difference = " << x <<'\n';
}